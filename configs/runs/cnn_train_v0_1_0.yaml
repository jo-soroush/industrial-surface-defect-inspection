identity:
  run_config_id: cnn_train_v0_1_0
  description: Resolved training run config for the CNN classification baseline.
  task_type: classification
  track_id: classification

model_identity:
  model_name: cnn
  model_type: cnn
  model_version: 0.1.0
  model_config_id: cnn_config

dataset_binding:
  dataset_id: mvtec_classification
  dataset_version: mvtec_1.0
  split_manifest_path: null

preprocessing:
  preprocessing_policy_path: configs/data/preprocessing_mvtec.yaml
  preprocessing_version: preprocessing_policy_1.0
  augmentation_policy_version: null

training_runtime:
  batch_size: 32
  learning_rate: 0.001
  epochs: 1
  optimizer: adam
  loss_function: cross_entropy
  seed: 42
  device: auto

checkpoint:
  enabled: true
  save_best: true
  save_last: true

early_stopping:
  enabled: false
  patience: 5

output_control:
  save_artifact: true
  save_metadata: true
  save_logs: true
  save_metrics: true
